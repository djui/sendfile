{application, sendfile,
 [{description, "sendfile linked-in driver"},
  {vsn, git},
  {modules, []},
  {applications, [
                  kernel,
                  stdlib
                 ]},
  {registered, [sendfile_drv]},
  {mod, {sendfile_app, []}},
  {env, []}
 ]}.

